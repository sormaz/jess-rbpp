(deftemplate ProcessFeatureRelation
       (slot oldFeature)
       (slot processCap)
       (slot status)
       (slot newFeature)
)

(deftemplate FeatureRelation
       (slot Feature)
       (slot status)
)

(provide relationTemplate)